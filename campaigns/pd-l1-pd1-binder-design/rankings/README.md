# Public Candidate Summaries

This compatibility directory contains public summaries of candidate-ranking
outputs.

Do not commit local `candidate_ranking.*.local.json` files. Full runtime ranking
outputs can contain generated binder sequences, generated structure paths,
`.runtime` artifact locations, and provider closeout context. Public summaries
should keep only candidate IDs, high-level metrics, result boundaries, and
explicit redaction flags.
