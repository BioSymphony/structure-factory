# Public Jury Summaries

This directory contains public-safe summaries of candidate jury outputs.

Do not commit local `candidate_jury.*.local.json` files. Full runtime juries can
contain generated binder sequences, generated structure paths, `.runtime`
artifact locations, and provider closeout context. Public summaries should keep
only candidate IDs, high-level metrics, claim ceilings, and explicit redaction
flags.
