# Changelog

## Unreleased

Public-prep pass on the local repo. No remote push.

- Reframed the README, agent skills, AGENTS guide, FAQ, glossary, and tool cards to lead with what users and their agents can run, rather than what the repo is not.
- Added `bsf catalog` (JSON and Markdown) and wired it into the Make targets and skill files.
- Added new tool cards for Boltz, Chai-1, and ProteinMPNN drafted from upstream sources, plus a `Hand A Mission To An Agent` section to every existing tool card.
- Added `docs/faq.md` and `docs/glossary.md` so newcomers and general-purpose agents have a starting place for jargon and common questions.
- Scrubbed operational data: removed real spend numbers, run-specific dates, internal infrastructure naming (`bsf-structure-factory-v0` network volume, `gpcr-activation-atlas-v0` branch), iteration markers (v11/v12/v13), and the in-progress capstone HTML generator that embedded session pain points.
- Replaced specific budget caps and per-pod cost defaults with `operator-defined` or `0` placeholders across bridge manifests, builder scripts, and planning docs.
- Replaced campaign-specific candidate-jury scores with synthetic schema-example values; the sequence content was already redacted upstream.
- Stripped internal codes: F-codes (RunPod platform footgun labels F19/F20/F21/F22/F23/F25) became descriptive labels (sticky-pre-RUNNING, payload-size-rejection, proxy-URL-lag, etc.); T-prefix Linear issue IDs (T00, T01, T02, T03, T-UMBRELLA) were renamed to descriptive labels and the corresponding files were renamed.
- Slimmed campaign baggage. Deleted ~165 files (666 → 501 tracked) of internal-execution detail that came along when the repo was repurposed as a public agent skill: 41 of the 48 GPCR Linear-issue shard templates (kept 7 representative, one per wave kind), 81 of the 99 RunPod bridge manifests (kept 18, representative shards for each wave plus the non-GPCR campaigns), 26 of the 42 other Linear issues across cryo-EM, screening, and supercharger campaigns (kept ~7 representatives), and the 6 internal planning docs from `campaigns/pd-l1-pd1-binder-design/` (`BOLTZ_VALIDATION_PLAN.md`, `GENIE3_WAVE_PLAN.md`, `CAPSTONE_OUTLINE.md`, `REPORT_LAYOUT_PLAN.md`, `RFDIFFUSION_INTEGRATION_PLAN.md`, `DEMO_OVERVIEW.md`). The `rfdiffusion-designs-v13-canary/` directory was renamed to `rfdiffusion-designs/`. The remaining content shows the shape of each campaign without the per-shard combinatorial expansion.
- Removed the entire GPCR Activation Atlas surface (501 → 410 tracked, ~91 more files). It was an operator-internal multi-wave demo that read more like a small research paper (Quarto book, hero figures, W4 results report with real Boltz pocket-IoU findings on 14 GPCR shards) than a public agent-skill demo. Deleted `demos/gpcr-activation-atlas/` (full Quarto book + 6 hero PNGs + W1/W2/W4 result narratives), `campaigns/multimer-state-atlas/` (issue-dag + 7 representative wave issues), 15 `modules/data-modules/gpcr-*.v1.json` files, the GPCR campaign + artifact-contract modules, 2 references files, 7 GPCR bridge manifests, 5 GPCR stage contracts, 5 GPCR entrypoints, 4 GPCR launch manifests, and 12 GPCR scripts (build_gpcr_*, gpcr_*, build_w*_manifest, ai_design_stack_check, aggregate_w4_results, render_w4_hero_shots, rescore_w4_pocket_iou). Updated cross-references in README, AGENTS, FAQ, capabilities, use-cases, agent-recipes, agent-run-learnings, agentic-biology-harness, architecture, genie3-boltz-demo-ideas, io-gap-and-mainline-plan, workflow-map, the cryoem/pd-l1/telomerase READMEs, lane and image modules, software-registry, the supercharger receptor-state-registry and design-tranche fixtures, the Makefile (dropped the GPCR-specific stack/runtime check targets and the four demo-prep-check targets), and the test suite. The skill still supports GPCR / multimer state atlas campaigns as a capability; it just no longer ships one operator's executed instance.
- Removed the telomerase holoenzyme binder-design campaign and the structure-factory-supercharger campaign (~44 more files, 410 → 366 tracked). Same principle as the GPCR removal: things tested in the private sister repo to validate the skill should not ship; only the learnings. Telomerase was an operator-executed 10-hour bounded run (campaign README with Quick-status table, ten-hour-agent-handoff, 2 Linear issue drafts, 9 dedicated scripts including monitor.sh/status.py/stretch_monitor.sh, a stage contract, a bridge manifest, an RNP schema, 2 supercharger fixture files, a render pipeline test, and a hero report assembler). Supercharger was a meta-orchestration program for shipping the skill itself (6 campaign files plus the supercharger_contract_check.py). Also trimmed the 11 PD-L1 dedicated runner scripts (`pd_l1_*`, `build_pd_l1_*`, `assemble_pd_l1_*`, `preflight_pd_l1`, `slice_pd_l1_target`, `enumerate_pd_l1_interface`, `recompute_pd_l1_dossier_distances`); the PD-L1 campaign folder and its launch packets stay as a template shape. Removed the `protein_rna_fit` mode and `telomerase_manifest()` from structure_intent_compile.py, the TELOMERASE-PROTEIN-RNA-FIT issue draft from screening_issue_broker.py, telomerase schema entries from screening_schema_check.py, and the telomerase tag/branch from genie3_config_synthesizer.py. Deleted render_binder_videos.py and render_binder_panels.py (telomerase-specific despite generic names). Cleaned cross-refs in ai-design-runtime-readiness, agent-run-learnings (reframed the "first end-to-end Genie 3 + Boltz dossier" section as target-agnostic operational lessons), genie3-boltz-demo-ideas (dropped Demo 6: Telomerase Holoenzyme Binder Hunt), io-gap-and-mainline-plan, screening-superpowers, references/software-registry.yaml, campaigns/README, the screening campaign issue-dag, the Makefile (dropped `telomerase-status` and `demo-telomerase-mvd` targets), and the test suite.

## 0.1.0-alpha.0 - 2026-05-13

Initial public Structure Factory export.

- Added public BioSymphony/Symphony harness for structural biology campaign planning.
- Added Codex-compatible and portable Structure Factory skills.
- Added public PD-L1 binder-design example with claim-capped candidate jury shape.
- Added tracker-neutral issue pack for the binder-design fast path.
- Added RunPod-first launch templates, stage contracts, and provider docs.
- Added dependency-free public validator and audit CLI.
- Added `make harness-check`, `make release-check`, and optional `make secret-scan` gates.
- Removed private history, private local runtime artifacts, concrete provider IDs, credentials, raw/generated structures, archives, videos, and model weights from the public export.
