# Visual Asset Notes

These SVGs are public-safe, text-based diagrams for README and quickstart use. They contain no private images, generated structures, provider data, or external dependencies.

## `structure-factory-loop.svg`

Text equivalent: public biological intent becomes a target/data contract, then agent lanes, then local and operator gates, then evidence artifacts, candidate juries, and claim-capped reports.

Use this when introducing the overall Structure Factory workflow.

## `newcomer-paths.svg`

Text equivalent: newcomers can start with the CLI for a local scaffold, the agent skill for planned multi-step work, or recipes for tested workflows.

Use this when explaining how a person or agent should begin.

## `workflow-ladder.svg`

Text equivalent: local scaffold leads to an issue pack, then a cloud contract, then an operator-gated run outside public git, then verified closeout.

Use this when explaining why the public repo is useful even before any provider run.
