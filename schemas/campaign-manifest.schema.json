{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/BioSymphony/biosymphony-structure-factory-public/schemas/campaign-manifest.schema.json",
  "title": "BioSymphony Structure Factory Campaign Manifest",
  "type": "object",
  "required": ["campaign_id", "claim_level", "system", "target", "lanes", "expected_artifacts"],
  "properties": {
    "campaign_id": {"type": "string", "minLength": 1},
    "title": {"type": "string"},
    "claim_level": {
      "type": "string",
      "enum": ["planning", "public_demo", "public_synthetic_demo", "computational_candidate", "candidate", "blocked", "insufficient_evidence"]
    },
    "wet_lab_execution": {"type": "boolean"},
    "therapeutic_claims": {"type": "boolean"},
    "system": {
      "type": "object",
      "required": ["privacy"],
      "properties": {
        "privacy": {"type": "string", "enum": ["public_only", "public_or_synthetic_only", "synthetic_demo"]}
      }
    },
    "target": {
      "type": "object",
      "required": ["label", "public_accession", "window"],
      "properties": {
        "label": {"type": "string"},
        "public_accession": {"type": "string"},
        "window": {"type": "string"}
      }
    },
    "lanes": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "purpose", "claim_ceiling"],
        "properties": {
          "id": {"type": "string"},
          "purpose": {"type": "string"},
          "claim_ceiling": {"type": "string"}
        }
      }
    },
    "expected_artifacts": {
      "type": "array",
      "items": {"type": "string"}
    }
  }
}
